<%= form_with(model: expence) do |form| %>
  <% if expence.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expence.errors.count, "error") %> prohibited this expence from being saved:</h2>

      <ul>
        <% expence.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount %>
  </div>

  <div>
    <%= form.label :approved_by, style: "display: block" %>
    <%= form.text_field :approved_by %>
  </div>

  <div>
    <%= form.label :approval_status, style: "display: block" %>
    <%= form.text_field :approval_status %>
  </div>

  <div>
    <%= form.label :issue_id, style: "display: block" %>
    <%= form.text_field :issue_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
